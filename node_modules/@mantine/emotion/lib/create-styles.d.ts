import { CSSObject } from '@emotion/serialize';
import { MantineBreakpoint, MantineTheme } from '@mantine/core';
export declare function getStylesRef(refName: string): string;
export declare const getHelpers: (theme: MantineTheme) => {
    light: string;
    dark: string;
    rtl: string;
    ltr: string;
    notRtl: string;
    notLtr: string;
    ref: typeof getStylesRef;
    smallerThan: (breakpoint: MantineBreakpoint | number) => string;
    largerThan: (breakpoint: MantineBreakpoint | number) => string;
};
export type EmotionHelpers = ReturnType<typeof getHelpers>;
export declare function createStyles<Key extends string = string, Params = void, Input extends Record<Key, CSSObject> = Record<Key, CSSObject>>(input: ((theme: MantineTheme, params: Params, helpers: EmotionHelpers) => Input) | Input): (params: Params) => {
    classes: { [key in keyof Input]: string; };
    cx: (...args: any) => string;
    theme: MantineTheme;
};
